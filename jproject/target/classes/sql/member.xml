<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberns">

				  
	 <!-- 회원저장 -->
    <insert id="memberJoin" parameterType="mb">
    	insert into joinMember (id, passwd, nickname, age, gender, post, address1, 
    	address2, email, phone, info, register, memberpoint, state, delcont, deldate)
  		values(join_member_joincode_seq.nextval,
     	#{id},#{passwd},#{nickname},#{age},#{gender},#{post},#{address1},#{address2},
     	#{email},#{phone},#{info},#{register},#{memberpoint},#{state},
     	#{delcont},#{deldate},#{profile, jdbcType=VARCHAR},sysdate,1)
    </insert>
    
				         
	<!-- 로그인 인증 -->
	<select id="memberlogin" parameterType="String" resultType="mb">
     select * from joinMember where id=#{id} and state=1
    </select>
    
	
	 <!-- 비밀번호 검색 -->
    <select id="pwdFind" parameterType="String" resultType="mb">
     select *  from joinMember where id=#{id} and name=#{name}
    </select>
</mapper>